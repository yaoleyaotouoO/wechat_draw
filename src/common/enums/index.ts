export enum PenWidthType {
    Small = 1,
    Medium = 2,
    Large = 3
}

export enum RoomStatus {
    Ready = 1,
    Running = 2
}

export enum UserStatus {
    Offline = 0,
    Ready = 1,
    InTheGame = 2
}